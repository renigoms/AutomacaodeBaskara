from CodigoBruto import calculadora_de_pixels

altura = float(input("Digite a altura em cm(se houver)"))
largura = float(input("digite a largura em cm(se houver)"))

calculadora_de_pixels(altura, largura)